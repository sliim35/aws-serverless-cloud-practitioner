{
  "name": "auth",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "compile": "tsc",
    "test": "npm run compile && npm run unit",
    "build": "sam build",
    "deploy": "sam deploy --no-confirm-changeset --stack-name auth --capabilities CAPABILITY_IAM --resolve-s3"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.92",
    "@types/node": "^18.11.4",
    "esbuild": "^0.14.14",
    "prettier": "^2.5.1",
    "typescript": "^4.8.4"
  }
}
